'use client';
import { useCallback, useEffect, useState } from 'react';
import { getSupabase } from '../../lib/supabase-browser';
import BrandSelector from './components/BrandSelector';

type Account={id:string;platform:string;name:string;handle:string|null;brand_id:string|null};type Metrics={source:string;accounts:Account[];stats:{totalPosts:number|null;reach:number|null;engagement:number|null;followers:number|null;trackedPublished:number};recentPosts:any[];warnings:string[]};
function formatNumber(v:number|null){if(v===null)return '—';if(v>=1000000)return `${(v/1000000).toFixed(1)}M`;if(v>=1000)return `${(v/1000).toFixed(v>=10000?0:1)}K`;return v.toLocaleString()}function formatDate(v:string){return new Date(v).toLocaleDateString(undefined,{day:'numeric',month:'short',year:'numeric'})}
export default function DashboardPage(){const[loading,setLoading]=useState(true),[metrics,setMetrics]=useState<Metrics|null>(null),[error,setError]=useState(''),[refreshing,setRefreshing]=useState(false),[selectedBrand,setSelectedBrand]=useState(''),[workspaceCount,setWorkspaceCount]=useState<number|null>(null);
useEffect(()=>{getSupabase().auth.getUser().then(({data,error})=>{if(error)setError(error.message);else if(!data.user){location.href='/login';return}setLoading(false)})},[]);
const handleWorkspacesLoaded=useCallback((workspaces:{id:string}[])=>setWorkspaceCount(workspaces.length),[]);
async function loadMetrics(brandId=selectedBrand){if(!brandId)return;try{setRefreshing(true);setError('');const s=(await getSupabase().auth.getSession()).data.session;if(!s){location.href='/login';return}const r=await fetch(`/api/dashboard/metrics?brandId=${encodeURIComponent(brandId)}`,{headers:{Authorization:`Bearer ${s.access_token}`},cache:'no-store'});const d=await r.json();if(!r.ok)throw new Error(d.error||'Unable to load live metrics.');setMetrics(d)}catch(e){setError(e instanceof Error?e.message:'Unable to load live metrics.')}finally{setRefreshing(false)}}
function changeBrand(id:string){setSelectedBrand(id);setMetrics(null);setError('');if(id)loadMetrics(id)}function go(href:string){location.href=href}
if(loading)return <main className="auth-page"><div className="muted">Loading workspace…</div></main>;
if(!selectedBrand)return <main className="auth-page"><section className="auth-card" style={{maxWidth:620,textAlign:'center'}}><div className="brand">SOCIAL MEDIA MANAGER</div><h1>Welcome to Social Media Manager</h1><p className="muted" style={{maxWidth:480,margin:'0 auto 24px'}}>Create or select a workspace to open only its accounts, live figures and publishing channels.</p><BrandSelector value={selectedBrand} onChange={changeBrand} onLoaded={handleWorkspacesLoaded}/>{workspaceCount===0?<div style={{marginTop:18}}><strong>No workspace yet</strong><p className="muted" style={{margin:'6px 0 14px'}}>Create your first workspace to get started.</p><button className="primary-btn" onClick={()=>go('/dashboard/brands')}>＋ Create Workspace</button></div>:null}{workspaceCount!==null&&workspaceCount>0?<p className="muted" style={{fontSize:11,marginTop:14}}>Choose a workspace above. Nothing else is loaded until you select one.</p>:null}</section></main>;
if(error&&!metrics)return <main className="auth-page"><section className="auth-card"><div className="brand">SOCIAL MEDIA MANAGER</div><h1>Live data unavailable</h1><p className="muted">{error}</p><button className="primary-btn" onClick={()=>loadMetrics()}>Retry</button></section></main>;
const stats=[['Total Posts',metrics?.stats.totalPosts,'Meta account totals','↗'],['Total Reach',metrics?.stats.reach,'Last 28 days','◉'],['Engagement',metrics?.stats.engagement,'Last 28 days','♡'],['Followers',metrics?.stats.followers,'Current total','♧'],['Tracked Posts',metrics?.stats.trackedPublished,'Published by this app','➤']];
return <main className="app-shell"><section className="main-shell" style={{width:'100%'}}><header className="main-header"><strong>Dashboard</strong><div className="header-actions"><button onClick={()=>loadMetrics()}>{refreshing?'…':'↻'}</button><button className="header-profile" onClick={()=>go('/dashboard/brands')}><div className="mini-logo">WS</div><strong>Workspace</strong><span>⌄</span></button></div></header><div className="dashboard-content"><div className="welcome-row"><div><h1>Workspace</h1><p>Selected workspace: live accounts, figures and publishing channels.</p></div><BrandSelector value={selectedBrand} onChange={changeBrand} onLoaded={handleWorkspacesLoaded}/></div><div className="live-row"><span className="live-pill"><i/> {metrics?.source==='meta_graph_api'?'Live Meta Data':'No live source'}</span><span className="selected-account">Selected Workspace</span></div><div className="stat-grid">{stats.map(([name,value,change,icon])=><article className="stat-card" key={name as string}><div className="stat-icon">{icon}</div><div><span>{name}</span><strong>{formatNumber(value as number|null)}</strong><small>{change}</small></div></article>)}</div>{metrics?.warnings?.length?<div className="data-warning">Some live metrics are unavailable for the selected workspace. No placeholder numbers are shown.</div>:null}<div className="channel-grid">{metrics?.accounts.length?metrics.accounts.map(a=><Channel key={a.id} platform={a.platform} title={a.platform==='instagram'?'Instagram':a.platform==='google_business'?'Google Business':'Facebook'} subtitle={a.platform==='instagram'?'Instagram Business Account':a.platform==='google_business'?'Business Profile':'Facebook Page'} name={a.name} detail={a.handle?`@${a.handle}`:'Connected'} tone={a.platform==='instagram'?'pink':a.platform==='google_business'?'green':'blue'} href={`/dashboard/publish?accountId=${a.id}`}/>):<div className="empty-card"><strong>No accounts assigned to this workspace</strong><span>Connect or assign Facebook, Instagram and Google Business accounts to this workspace.</span><button onClick={()=>go('/dashboard/accounts')}>Manage Accounts</button></div>}</div><div className="bottom-grid"><section className="recent-card"><div className="section-head"><div><span>LIVE CONTENT</span><h2>Recent Posts</h2></div><button onClick={()=>go('/dashboard/calendar')}>View all</button></div>{metrics?.recentPosts.length?metrics.recentPosts.map((p,i)=><div className="post-row" key={p.id}><div className={`post-thumb thumb-${i%4}`}>{p.platform==='Instagram'?'◎':p.platform==='Google Business'?'G':'f'}</div><div className="post-title"><strong>{p.title}</strong><small>{p.platform} · {formatDate(p.date)}</small></div><span>{formatNumber(p.reach)}</span><span>{formatNumber(p.engagement)}</span><em>Live</em><b>⋮</b></div>):<div className="empty-state"><strong>No recent posts returned by the selected workspace.</strong><span>Publish a post or connect an account to see live content here.</span></div>}</section><aside className="right-column"><div className="quick-card"><span className="quick-icon">✎</span><div><strong>Quick Create Post</strong><small>Publish to the selected workspace accounts</small></div><button onClick={()=>go(`/dashboard/publish?brandId=${selectedBrand}`)}>＋ Create New Post</button></div><div className="summary-card"><div className="section-head"><div><span>LIVE OVERVIEW</span><h2>Current Metrics</h2></div><button className="text-btn" onClick={()=>loadMetrics()}>{refreshing?'Updating…':'Refresh'}</button></div><Summary label="Total Posts" value={formatNumber(metrics?.stats.totalPosts??null)}/><Summary label="Total Reach" value={formatNumber(metrics?.stats.reach??null)}/><Summary label="Engagement" value={formatNumber(metrics?.stats.engagement??null)}/><Summary label="Followers" value={formatNumber(metrics?.stats.followers??null)}/></div></aside></div></div></section></main>}
function Channel({platform,title,subtitle,name,detail,tone,href}:{platform:string;title:string;subtitle:string;name:string;detail:string;tone:string;href:string}){return <article className="channel-card"><div className={`channel-icon ${tone}`}>{platform==='instagram'?'◎':platform==='google_business'?'G':'f'}</div><div className="channel-head"><div><h2>{title}</h2><small>{subtitle}</small></div><span className="connected-pill">✓ Connected</span></div><div className="channel-account"><div className="mini-logo">WS</div><div><strong>{name}</strong><small>{detail}</small></div></div><button className={`channel-action ${tone}`} onClick={()=>location.href=href}>Create / Manage <b>›</b></button></article>}
function Summary({label,value}:{label:string;value:string}){return <div className="summary-row"><span>◉</span><strong>{label}</strong><b>{value}</b></div>}
