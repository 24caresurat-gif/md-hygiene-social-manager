'use client';
import { useEffect, useState } from 'react';
import AppShell from '../components/AppShell';
import { getSupabase } from '../../../lib/supabase-browser';

type Account={id:string;platform:string;name:string;handle:string;status:string}; type FacebookPage={id:string;name:string;handle:string|null};
export default function AccountsPage(){
 const [accounts,setAccounts]=useState<Account[]>([]),[pages,setPages]=useState<FacebookPage[]>([]),[selected,setSelected]=useState(''),[loading,setLoading]=useState(true),[busy,setBusy]=useState(false),[error,setError]=useState(''),[success,setSuccess]=useState('');
 async function load(){const c=getSupabase();const{data:{user}}=await c.auth.getUser();if(!user){location.href='/login';return}const{data,error}=await c.from('social_accounts').select('id,platform,name,handle,status').eq('user_id',user.id).order('created_at',{ascending:false});if(error)setError(error.message);else setAccounts((data||[]) as Account[]);setLoading(false)}
 useEffect(()=>{const p=new URLSearchParams(location.search);if(p.get('error'))setError(p.get('error')||'');load();if(p.get('facebook')==='connected')loadPages()},[]);
 async function loadPages(){setBusy(true);try{const r=await fetch('/api/meta/facebook/pages',{cache:'no-store'}),d=await r.json();if(!r.ok)throw Error(d.error||'Unable to load Pages');setPages(d.pages||[]);if(d.pages?.length===1)setSelected(d.pages[0].id)}catch(e){setError(e instanceof Error?e.message:'Unable to load Pages')}finally{setBusy(false)}}
 async function connect(){if(!selected){setError('Please select a Facebook Page.');return}setBusy(true);try{const c=getSupabase(),{data:{session}}=await c.auth.getSession();const r=await fetch('/api/meta/facebook/connect',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${session?.access_token||''}`},body:JSON.stringify({pageId:selected})}),d=await r.json();if(!r.ok)throw Error(d.error||'Connection failed');setSuccess(`Connected ${d.page?.name||'Facebook Page'} successfully.`);setPages([]);setSelected('');load()}catch(e){setError(e instanceof Error?e.message:'Connection failed')}finally{setBusy(false)}}
 return <AppShell title="Accounts"><div className="page-head"><div><span className="eyebrow">CHANNELS</span><h1>Connected Accounts</h1><p>Connect and manage all your social channels from one workspace.</p></div><button className="btn btn-primary" onClick={()=>location.href='/api/meta/facebook/login'}>＋ Connect Facebook</button></div>
 {error&&<div className="alert alert-error">⚠ {error}<button onClick={()=>setError('')}>×</button></div>}{success&&<div className="alert alert-success">✓ {success}</div>}
 <div className="account-platform-grid"><Platform icon="f" tone="facebook" title="Facebook" text="Pages, publishing and account management." connected={accounts.some(a=>a.platform==='facebook')}/><Platform icon="◎" tone="instagram" title="Instagram" text="Professional account publishing and insights." connected={accounts.some(a=>a.platform==='instagram')}/><Platform icon="G" tone="google" title="Google Business" text="Business profile management and updates." connected={accounts.some(a=>a.platform==='google_business')}/></div>
 {pages.length>0&&<section className="panel connect-panel"><div><span className="eyebrow">META BUSINESS</span><h2>Select Facebook Page</h2><p>Choose which Page you want MD Hygiene Social Manager to manage.</p></div><select value={selected} onChange={e=>setSelected(e.target.value)}><option value="">Select a Page…</option>{pages.map(p=><option key={p.id} value={p.id}>{p.name}{p.handle?` (@${p.handle})`:''}</option>)}</select><button className="btn btn-primary" disabled={!selected||busy} onClick={connect}>{busy?'Connecting…':'Connect Selected Page'}</button></section>}
 <section className="panel"><div className="panel-head"><div><span className="eyebrow">YOUR CHANNELS</span><h2>Connected accounts</h2></div><span className="count-badge">{accounts.length} connected</span></div>{loading?<div className="empty-state">Loading accounts…</div>:accounts.length===0?<div className="empty-state"><div className="empty-icon">◎</div><h3>No accounts connected</h3><p>Connect your Facebook Page to get started.</p><button className="btn btn-primary" onClick={()=>location.href='/api/meta/facebook/login'}>Connect Facebook</button></div>:<div className="account-list">{accounts.map(a=><div className="account-row" key={a.id}><div className={`account-avatar ${a.platform}`}>{a.platform==='facebook'?'f':a.platform==='instagram'?'◎':'G'}</div><div className="account-info"><strong>{a.name}</strong><span>{a.handle||a.platform}</span></div><span className="connected-pill">● Connected</span><button className="btn btn-soft" onClick={()=>location.href='/dashboard/publish'}>Create Post →</button></div>)}</div>}</section></AppShell>}
function Platform({icon,tone,title,text,connected}:{icon:string;tone:string;title:string;text:string;connected:boolean}){return <article className="platform-card"><div className={`platform-icon ${tone}`}>{icon}</div><div className="platform-title"><div><h2>{title}</h2><span>{text}</span></div><span className={connected?'connected-pill':'soon-pill'}>{connected?'Connected':'Available'}</span></div><button className="btn btn-soft" onClick={()=>title==='Facebook'&&(location.href='/api/meta/facebook/login')}>{connected?'Manage Account':'Connect'} <b>→</b></button></article>}
