'use client';
import {useEffect,useState} from 'react';
import AppShell from '../components/AppShell';
export default function CalendarPage(){const[id,setId]=useState('');const[date,setDate]=useState(()=>new Date().toISOString().slice(0,10));const[notice,setNotice]=useState('');useEffect(()=>{const saved=localStorage.getItem('mdsm:selectedWorkspaceId')||'';if(!saved)location.href='/dashboard';else setId(saved)},[]);const openPost=()=>{if(!id)return;location.href=`/dashboard/publish?brandId=${encodeURIComponent(id)}&scheduledDate=${encodeURIComponent(date)}`};return <AppShell title="Calendar"><div className="page-head"><div><span className="eyebrow">PLANNING</span><h1>Content Calendar</h1><p>Plan and review content for this workspace.</p></div><button className="btn btn-primary" onClick={openPost}>＋ Schedule Post</button></div><section className="panel" style={{padding:24}}><div className="section-head"><div><span>WORKSPACE</span><h2>Publishing Planner</h2></div><input type="date" value={date} onChange={e=>setDate(e.target.value)} /></div><div className="calendar-empty" style={{marginTop:20}}><div className="empty-icon">□</div><h2>No scheduled posts yet</h2><p>Choose a date and create a scheduled post. Scheduled content for the selected workspace will appear here.</p><button className="btn btn-primary" onClick={openPost}>＋ Create Scheduled Post</button></div></section>{notice&&<p>{notice}</p>}</AppShell>}
