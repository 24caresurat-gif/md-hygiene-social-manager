'use client';
import {useEffect,useState} from 'react';
type Field={label:string;type?:string;required?:boolean;options?:string[]};
type Data={request:{id:string;name:string|null;email:string|null;phone:string|null;status:string;message:string|null};profile:{business_name:string;review_url:string|null;address:string|null}|null;form:{id:string;name:string;title:string|null;description:string|null;fields:Field[]|any;active:boolean}|null};
export default function PublicReviewRequest(){
 const[token,setToken]=useState(''),[data,setData]=useState<Data|null>(null),[rating,setRating]=useState(0),[answers,setAnswers]=useState<Record<string,string>>({}),[name,setName]=useState(''),[email,setEmail]=useState(''),[submitted,setSubmitted]=useState(false),[message,setMessage]=useState(''),[loading,setLoading]=useState(true),[busy,setBusy]=useState('');
 useEffect(()=>{const t=new URLSearchParams(location.search).get('token')||'';setToken(t);(async()=>{try{const r=await fetch('/api/public/review-request?token='+encodeURIComponent(t));const d=await r.json();if(!r.ok)throw Error(d?.error||'Unable to load request.');setData(d);setName(d.request?.name||'');setEmail(d.request?.email||'')}catch(e){setMessage(e instanceof Error?e.message:'Unable to load request.')}finally{setLoading(false)}})()},[]);
 async function submit(){setBusy('feedback');setMessage('');try{const r=await fetch('/api/public/review-request',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,action:'feedback',rating,answers,name,email})});const d=await r.json();if(!r.ok)throw Error(d?.error||'Unable to submit feedback.');setSubmitted(true);setMessage('Thank you. Your feedback has been received.')}catch(e){setMessage(e instanceof Error?e.message:'Unable to submit feedback.')}finally{setBusy('')}}
 async function google(){setBusy('google');setMessage('');try{const r=await fetch('/api/public/review-request',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,action:'google_click'})});const d=await r.json();if(!r.ok)throw Error(d?.error||'Google review link is unavailable.');if(d.redirect_url)location.assign(d.redirect_url);else setMessage('Google review link is not available for this location yet.')}catch(e){setMessage(e instanceof Error?e.message:'Unable to open Google review.')}finally{setBusy('')}}
 if(loading)return <main className='page'><div className='card'>Loading review request…</div></main>;
 if(!data)return <main className='page'><div className='card'><h1>Review request</h1><p>{message||'This review request is unavailable.'}</p></div></main>;
 const fields:Array<Field>=Array.isArray(data.form?.fields)?data.form!.fields:[];
 return <main className='page'><style jsx>{'.page{min-height:100vh;display:grid;place-items:center;padding:24px;background:linear-gradient(145deg,#f7fafb,#edf8f6);font-family:Inter,ui-sans-serif,system-ui,sans-serif;color:#17202b}.card{width:min(680px,100%);background:#fff;border:1px solid #e3eaee;border-radius:22px;padding:28px;box-shadow:0 22px 70px rgba(15,23,42,.10)}.eyebrow{font-size:10px;letter-spacing:.12em;font-weight:900;color:#087f7b}.card h1{margin:8px 0;font-size:30px}.muted{color:#667085;font-size:12px;line-height:1.6}.stars{display:flex;gap:6px;margin:22px 0}.star{width:48px;height:44px;border:1px solid #dce5e8;border-radius:10px;background:#fff;font-size:20px;cursor:pointer}.on{background:#fff7df;border-color:#e8c86f;color:#a56b00}.field{display:grid;gap:7px;margin-top:14px}.field label{font-size:10px;font-weight:850;color:#475467}.field input,.field textarea,.field select{width:100%;border:1px solid #dbe4e8;border-radius:10px;padding:11px 12px;background:#fff;min-height:42px}.field textarea{min-height:100px;resize:vertical}.actions{display:flex;gap:9px;flex-wrap:wrap;margin-top:20px}.btn{border:0;border-radius:10px;padding:11px 15px;font-weight:850;cursor:pointer}.primary{background:#087f7b;color:#fff}.soft{background:#edf8f7;color:#087f7b}.btn:disabled{opacity:.5;cursor:wait}.message{margin-top:15px;padding:11px 12px;border-radius:10px;background:#f6f8fa;color:#475467;font-size:11px}.thanks{padding:18px;background:#edf8f1;border-radius:12px;color:#14804a;font-weight:750;font-size:12px}'}</style><div className='card'><div className='eyebrow'>CUSTOMER FEEDBACK</div><h1>{data.form?.title||'How was your experience with '+(data.profile?.business_name||'our business')+'?'}</h1><p className='muted'>{data.form?.description||data.request.message||'We value your feedback and appreciate your time.'}</p><div className='stars'>{[1,2,3,4,5].map(n=><button key={n} className={'star '+(rating>=n?'on':'')} onClick={()=>setRating(n)} aria-label={n+' star'}>★</button>)}</div>{!submitted&&rating>0&&<><div className='field'><label>Your name</label><input value={name} onChange={e=>setName(e.target.value)} placeholder='Your name'/></div><div className='field'><label>Email</label><input type='email' value={email} onChange={e=>setEmail(e.target.value)} placeholder='you@example.com'/></div>{fields.map((f,i)=><div className='field' key={i}><label>{f.label}{f.required?' *':''}</label>{f.type==='textarea'?<textarea value={answers[f.label]||''} onChange={e=>setAnswers({...answers,[f.label]:e.target.value})}/>:f.type==='select'?<select value={answers[f.label]||''} onChange={e=>setAnswers({...answers,[f.label]:e.target.value})}><option value=''>Select…</option>{(f.options||[]).map(o=><option key={o} value={o}>{o}</option>)}</select>:<input value={answers[f.label]||''} onChange={e=>setAnswers({...answers,[f.label]:e.target.value})}/>}</div>)}<div className='actions'><button className='btn soft' onClick={()=>void submit()} disabled={busy!==''}>{busy==='feedback'?'Sending…':'Send Feedback'}</button>{data.profile?.review_url&&<button className='btn primary' onClick={()=>void google()} disabled={busy!==''}>{busy==='google'?'Opening…':'Leave a Google Review'}</button>}</div></>}{submitted&&<div className='thanks'>Thank you — your feedback has been received.</div>}{message&&<div className='message'>{message}</div>}<p className='muted' style={{marginTop:20}}>{data.profile?.address||''}</p></div></main>;
}