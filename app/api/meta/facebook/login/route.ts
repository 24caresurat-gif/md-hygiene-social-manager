import {NextResponse} from 'next/server';
const DEFAULT_META_APP_ID='1578165993688458',DEFAULT_META_CONFIG_ID='1857783405201550';
const META_PAGE_SCOPES='pages_show_list,pages_read_engagement,pages_read_user_content,pages_manage_metadata,pages_manage_posts';
export async function GET(request:Request){const url=new URL(request.url),origin=url.origin,appId=process.env.META_APP_ID||DEFAULT_META_APP_ID,configId=process.env.META_CONFIG_ID||DEFAULT_META_CONFIG_ID,brandId=url.searchParams.get('brandId')||'';if(!appId||!configId)return NextResponse.json({error:'Meta configuration is missing.'},{status:500});const redirectUri=`${origin}/api/meta/facebook/callback`,state=crypto.randomUUID(),oauth=new URL('https://www.facebook.com/v23.0/dialog/oauth');oauth.searchParams.set('client_id',appId);oauth.searchParams.set('redirect_uri',redirectUri);oauth.searchParams.set('state',state);oauth.searchParams.set('config_id',configId);oauth.searchParams.set('response_type','code');oauth.searchParams.set('scope',META_PAGE_SCOPES);oauth.searchParams.set('auth_type','rerequest');const response=NextResponse.redirect(oauth);response.cookies.set('meta_oauth_state',state,{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'lax',path:'/',maxAge:600});if(brandId)response.cookies.set('workspace_brand_id',brandId,{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'lax',path:'/',maxAge:600});return response;}
