import { NextResponse } from 'next/server';
import { createClient } from '@supabase/supabase-js';
function client(token:string){const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key)throw new Error('Supabase configuration is missing.');return createClient(url,key,{global:{headers:{Authorization:`Bearer ${token}`}}})}
export async function GET(request:Request){try{const token=request.headers.get('authorization')?.replace(/^Bearer\s+/i,'');if(!token)return NextResponse.json({error:'Authentication required.'},{status:401});const sb=client(token);const {data:u,error:ue}=await sb.auth.getUser(token);if(ue||!u.user)return NextResponse.json({error:'Invalid session.'},{status:401});const {data,error}=await sb.from('workplace_members').select('workplace_id,brands(id,name,slug,logo_url),workplace_permissions(platform,can_view,can_create,can_edit,can_publish)').eq('user_id',u.user.id);if(error)throw error;return NextResponse.json({workspaces:data||[]})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Unable to load assigned workspaces.'},{status:500})}}
