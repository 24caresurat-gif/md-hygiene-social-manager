import {NextResponse} from 'next/server';import {createClient} from '@supabase/supabase-js';
async function auth(request:Request){const token=request.headers.get('authorization')?.replace(/^Bearer\s+/i,'');if(!token)throw new Error('Authentication required.');const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key)throw new Error('Supabase configuration is missing.');const db=createClient(url,key,{global:{headers:{Authorization:`Bearer ${token}`}}});const {data,error}=await db.auth.getUser(token);if(error||!data.user)throw new Error('Invalid session.');return {db,userId:data.user.id}}
export async function GET(request:Request){try{const {db,userId}=await auth(request),url=new URL(request.url),brandId=url.searchParams.get('brandId'),status=url.searchParams.get('status');let q=db.from('publishing_history').select('*').eq('user_id',userId).order('created_at',{ascending:false}).limit(100);if(brandId)q=q.eq('brand_id',brandId);if(status)q=q.eq('status',status);const {data,error}=await q;if(error)throw error;return NextResponse.json({history:data||[]})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Unable to load publishing history.'},{status:400})}}
