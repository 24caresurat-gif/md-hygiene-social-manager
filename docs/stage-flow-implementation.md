# Stage flow implementation notes

Stage 1: Login -> authenticated users enter the Workspace Hub.
Stage 2: Workspace Hub -> workspace cards. Admins can create; staff only see assigned cards.
Stage 3: Create Workspace -> name + optional logo + submit; success returns to Stage 2.
